server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        encrypt.enabled: true
        git:
          uri: https://github.com/ds-tothemoon/ecommerce-config
